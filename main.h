#ifndef MAIN_H
#define MAIN_H

#include <limits.h>
#include <stdarg.h>
#include <stdlib.h>
#include <unistd.h>
#include <stdio.h>

int print_s(char *s);
int _putchar(char c);
int _printf(const char *format, ...);
int len(int num);
int print_di(int num);

#endif
